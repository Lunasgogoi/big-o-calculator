# Big O Calc Frontend

React frontend for Big O Calc, built with Vite and JavaScript/JSX.

## Scripts

```sh
npm run dev
npm run build
npm run lint
npm run preview
```

The app entry point is `src/main.jsx`, and Vite is configured in `vite.config.js`.
